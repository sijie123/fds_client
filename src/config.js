const SERVER_IP = "54.169.81.205"; // Public facing IP. Use 127.0.0.1 if hosting locally.
const BACKEND_PORT = 3001; // The port where the backend server is located.

let config = {
    SERVER_IP: SERVER_IP,
    BACKEND_PORT: BACKEND_PORT
};

export default config;